#include <bits/stdc++.h>
using namespace std;
#define int long long

void solve() {
    
}

int32_t main() {
    cin.tie(0)->sync_with_stdio(0);
    cout << setprecision(20) << fixed;

    int T = 1;
    cin >> T;
    for(int I = 1; I <= T; I++) {
        solve();
    }

    return 0;
}
